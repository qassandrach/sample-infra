apiVersion: 1

providers:
  - name: dashboards
    # folder: Cluster-2 # The folder where to place the dashboards
    type: file
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: true
